# 今日のGit学習メモ
初めてのコミットに成功しました！Vimエディタからの脱出も経験。

## 覚えた基本のルーティン
1. `git init`：リポジトリの初期化（最初だけ）
2. `touch disk_usage.py`：ファイルの作成
3. `git add disk_usage.py`：保存対象の登録（ステージング）
4. `git status`：状態の確認
5. `git commit -m "最初のコミット"`：エディタを開かずに一発でコミット
